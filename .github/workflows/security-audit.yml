name: 🔒 安全审计

on:
  schedule:
    - cron: '0 2 * * 1'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  security-audit:
    name: 安全审计
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: 📥 检出代码
        uses: actions/checkout@v4
      
      - name: 📦 设置 Node.js 环境
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: 'package-lock.json'
      
      - name: 📥 安装项目依赖
        run: npm ci
      
      - name: 🔍 检查依赖漏洞
        run: npm audit --audit-level=moderate
        continue-on-error: true
      
      - name: 🔍 代码安全扫描
        uses: github/codeql-action/analyze@v3
        continue-on-error: true
      
      - name: 📊 生成安全审计报告
        if: always()
        run: |
          echo "## 🔒 安全审计报告" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "审计时间: $(date '+%Y-%m-%d %H:%M:%S')" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 检查项目" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ 依赖漏洞扫描" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ 代码安全扫描" >> $GITHUB_STEP_SUMMARY
